#ifndef _TYPEDEF_H
#define _TYPEDEF_H

#include <map>
#include <string>
#include <vector>

typedef std::string Token;
typedef std::vector<Token> TokenVec;
typedef std::map<Token, bool> Context;

#endif
